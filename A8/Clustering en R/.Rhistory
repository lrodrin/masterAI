BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
func <- function (var) {
new_data <- c("Cell.size", "Cell.shape", var)
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[new_data], centers = 3, nstart = 20)
fviz_cluster(km_clusters, data = BreastCancer.features[new_data])
}
vars <- c("Cl.thickness", "Marg.adhesion", "Epith.c.size", "Bare.nuclei", "Bl.cromatin")
func(vars[1])
func(vars[2])
func(vars[3])
func(vars[4])
func(vars[5])
#INSERTAR CÓDIGO AQUÍ
agrupamiento.jerarquico <- hclust(dist(BreastCancer.features), method = "single")
plot(agrupamiento.jerarquico)
# dendrogram <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(agrupamiento.jerarquico, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
#INSERTAR CÓDIGO AQUÍ
agrupamiento.jerarquico <- hclust(dist(BreastCancer.features), method = "single")
plot(agrupamiento.jerarquico)
dendrogram <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
#INSERTAR CÓDIGO AQUÍ
agrupamiento.jerarquico <- hclust(dist(BreastCancer.features), method = "single")
plot(agrupamiento.jerarquico)
dendrogram <- hclust(dist(BreastCancer.features, method = 'single'), method = 'ward.D')
#INSERTAR CÓDIGO AQUÍ
agrupamiento.jerarquico <- hclust(dist(BreastCancer.features), method = "single")
plot(agrupamiento.jerarquico)
dendrogram <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
#INSERTAR CÓDIGO AQUÍ
agrupamiento.jerarquico <- hclust(dist(BreastCancer.features), method = "single")
plot(agrupamiento.jerarquico)
dendrogram <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
fviz_dend(dendrogram, rect = TRUE) # dendrogam
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
# 2. Compute dissimilarity matrix
m <- dist(BreastCancer.features, method = "euclidean")
# Hierarchical clustering using Ward's method
hc <- hclust(m, method = "ward.D2" )
# Cut tree into 4 groups
grp <- cutree(hc, k = 2)
# Visualize
plot(res.hc, cex = 0.6) # plot tree
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
# 2. Compute dissimilarity matrix
m <- dist(BreastCancer.features, method = "euclidean")
# Hierarchical clustering using Ward's method
hc <- hclust(m, method = "ward.D2" )
# Cut tree into 4 groups
grp <- cutree(hc, k = 2)
# Visualize
plot(hc, cex = 0.6) # plot tree
rect.hclust(hc, k = 2, border = 2:5) # add rectangle
#INSERTAR CÓDIGO AQUÍ
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
agrupamientoJ <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
clases_aj <- cutree(agrupamientoJ, k = 2)
dataset$cluster <- clases_aj
knitr::opts_chunk$set(echo = TRUE)
library(mlbench)
library(caret)
library(factoextra)
library(ggdendro)
library(corrplot)
data(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
summary(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
sum(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
BreastCancer <- na.omit(BreastCancer)
any(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
for (i in 1:(ncol(BreastCancer) - 1))
BreastCancer[, i] <- as.numeric(as.character(BreastCancer[, i]))
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
BreastCancer$Id <- NULL
head(BreastCancer, 3)
# INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
str(BreastCancer.features, width = 85, strict.width = "cut")
# En este caso solo estamos mostrando el gráfico para las variables Cl.thickness y Cell.size.
ggplot(BreastCancer, aes(Cl.thickness, Cell.size, color = Class)) + geom_point()
#INSERTAR CÓDIGO AQUÍ
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
km_clusters
table(km_clusters$cluster, BreastCancer$Class)
#INSERTAR CÓDIGO AQUÍ
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
corrplot(cor(BreastCancer.features), method = "number", type = "lower")
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("center", "scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9],
method=c("center", "scale", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
func <- function (var) {
new_data <- c("Cell.size", "Cell.shape", var)
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[new_data], centers = 3, nstart = 20)
fviz_cluster(km_clusters, data = BreastCancer.features[new_data])
}
vars <- c("Cl.thickness", "Marg.adhesion", "Epith.c.size", "Bare.nuclei", "Bl.cromatin")
func(vars[1])
func(vars[2])
func(vars[3])
func(vars[4])
func(vars[5])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) + labs(title = "Dendrograma")
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
agrupamientoJ <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
clases_aj <- cutree(agrupamientoJ, k = 2)
dataset$cluster <- clases_aj
BreastCancer.features <- scale(BreastCancer.features[, 1:9])
agrupamientoJ <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
clases_aj <- cutree(agrupamientoJ, k = 2)
BreastCancer.features$cluster <- clases_aj
knitr::opts_chunk$set(echo = TRUE)
library(mlbench)
library(caret)
library(factoextra)
library(ggdendro)
library(corrplot)
data(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
summary(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
sum(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
BreastCancer <- na.omit(BreastCancer)
any(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
for (i in 1:(ncol(BreastCancer) - 1))
BreastCancer[, i] <- as.numeric(as.character(BreastCancer[, i]))
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
BreastCancer$Id <- NULL
head(BreastCancer, 3)
# INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
str(BreastCancer.features, width = 85, strict.width = "cut")
# En este caso solo estamos mostrando el gráfico para las variables Cl.thickness y Cell.size.
ggplot(BreastCancer, aes(Cl.thickness, Cell.size, color = Class)) + geom_point()
#INSERTAR CÓDIGO AQUÍ
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
km_clusters
table(km_clusters$cluster, BreastCancer$Class)
#INSERTAR CÓDIGO AQUÍ
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
corrplot(cor(BreastCancer.features), method = "number", type = "lower")
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("center", "scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9],
method=c("center", "scale", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
func <- function (var) {
new_data <- c("Cell.size", "Cell.shape", var)
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[new_data], centers = 3, nstart = 20)
fviz_cluster(km_clusters, data = BreastCancer.features[new_data])
}
vars <- c("Cl.thickness", "Marg.adhesion", "Epith.c.size", "Bare.nuclei", "Bl.cromatin")
func(vars[1])
func(vars[2])
func(vars[3])
func(vars[4])
func(vars[5])
res <- hcut(BreastCancer.features, k = 2, stand = TRUE)
# Visualize
fviz_dend(res, rect = TRUE, cex = 0.5)
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Dendrograma")
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
#hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
#ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
data <- scale(BreastCancer.features[, 1:9])
#INSERTAR CÓDIGO AQUÍ
#hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
#ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
data <- scale(BreastCancer.features[, 1:9])
# Dissimilarity matrix
d <- dist(data, method = "euclidean")
#INSERTAR CÓDIGO AQUÍ
#hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'ward.D')
#ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
hc_data <- scale(BreastCancer.features[, 1:9])
d <- dist(hc_data, method = "euclidean")
#INSERTAR CÓDIGO AQUÍ
hc_data <- scale(BreastCancer.features[, 1:9])
d <- dist(hc_data, method = "euclidean")
hc <- hclust(d, method = "complete")
plot(hc, cex = 0.6, hang = -1)
#INSERTAR CÓDIGO AQUÍ
hc_data <- scale(BreastCancer.features[, 1:9])
d <- dist(hc_data, method = "euclidean")
hc <- hclust(d, method = "complete")
plot(hc, cex = 0.6, hang = -1)
dendrogram <- hclust(dist(hc_data, method = 'euclidean'), method = 'complete')
ggdendrogram(dendrogram, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Dendrograma")
#INSERTAR CÓDIGO AQUÍ
hc_data <- scale(BreastCancer.features[, 1:9])
hc <- hclust(dist(hc_data, method = 'euclidean'), method = 'complete')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'complete')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'complete')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
knitr::opts_chunk$set(echo = TRUE)
library(mlbench)
library(caret)
library(factoextra)
library(ggdendro)
library(corrplot)
data(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
summary(BreastCancer)
#INSERTAR CÓDIGO AQUÍ
sum(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
BreastCancer <- na.omit(BreastCancer)
any(is.na(BreastCancer))
#INSERTAR CÓDIGO AQUÍ
for (i in 1:(ncol(BreastCancer) - 1))
BreastCancer[, i] <- as.numeric(as.character(BreastCancer[, i]))
str(BreastCancer, width = 85, strict.width = "cut")
#INSERTAR CÓDIGO AQUÍ
BreastCancer$Id <- NULL
head(BreastCancer, 3)
# INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
str(BreastCancer.features, width = 85, strict.width = "cut")
# En este caso solo estamos mostrando el gráfico para las variables Cl.thickness y Cell.size.
ggplot(BreastCancer, aes(Cl.thickness, Cell.size, color = Class)) + geom_point()
#INSERTAR CÓDIGO AQUÍ
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
km_clusters
table(km_clusters$cluster, BreastCancer$Class)
#INSERTAR CÓDIGO AQUÍ
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
corrplot(cor(BreastCancer.features), method = "number", type = "lower")
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("center", "scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9],
method=c("center", "scale", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("range", "YeoJohnson"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[, c(2, 3)], centers = 2, nstart = 20)
table(km_clusters$cluster, BreastCancer$Class)
fviz_cluster(km_clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
func <- function (var) {
new_data <- c("Cell.size", "Cell.shape", var)
set.seed(101)
km_clusters <- kmeans(BreastCancer.features[new_data], centers = 3, nstart = 20)
fviz_cluster(km_clusters, data = BreastCancer.features[new_data])
}
vars <- c("Cl.thickness", "Marg.adhesion", "Epith.c.size", "Bare.nuclei", "Bl.cromatin")
func(vars[1])
func(vars[2])
func(vars[3])
func(vars[4])
func(vars[5])
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
hc <- hclust(dist(BreastCancer.features, method = 'euclidean'), method = 'complete')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(BreastCancer.features$cluster, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(fviz_cluster(list(data = data, cluster = clust)), data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(fviz_cluster(list(data = data, cluster = clusters)), data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)]))
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features, cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = BreastCancer.features$cluster))
#INSERTAR CÓDIGO AQUÍ
BreastCancer.scale <- preProcess(BreastCancer[, 1:9], method=c("scale"))
BreastCancer.features <- predict(BreastCancer.scale, BreastCancer[, 1:9])
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
ggdendrogram(hc, rotate = FALSE, labels = FALSE, theme_dendro = TRUE) +
labs(title = "Cluster Dendrogram")
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(clusters, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(BreastCancer.features$cluster, data = BreastCancer.features[, c(2, 3)])
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
clusters <- cutree(hc, k = 3)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'ward.D2')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'ward.D2')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'single')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'average')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
hc <- hclust(dist(BreastCancer.features[, c(2, 3)], method = 'euclidean'), method = 'complete')
clusters <- cutree(hc, k = 2)
BreastCancer.features$cluster <- clusters
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
fviz_silhouette(hc)
#INSERTAR CÓDIGO AQUÍ
table(BreastCancer.features$cluster, BreastCancer$Class)
fviz_cluster(list(data = BreastCancer.features[, c(2, 3)], cluster = clusters))
fviz_silhouette(clusters)
